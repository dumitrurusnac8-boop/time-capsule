// Настройки капсулы. Это единственный файл, который надо править руками.
window.CAPSULE_CONFIG = {
  // Имя друга в родительном падеже ("для Саши"). Пусто — строка не показывается.
  friendName: "",

  // Даты в местном времени того, кто открывает страницу. Формат ГГГГ-ММ-ДД.
  sealedAt: "2026-09-19",
  opensAt: "2036-09-19",

  // Видеопослание. Файл кладётся рядом с index.html.
  video: "video.mp4",
  poster: "poster.jpg",
  // Размер кадра в пикселях — его печатает tools/compress_video.js.
  // Нужен, чтобы место под видео было верным ещё до загрузки файла.
  videoWidth: 720,
  videoHeight: 1280,

  // Папка с зашифрованным хранилищем (её создаёт tools/seal_vault.js).
  vaultDir: "vault",
};
